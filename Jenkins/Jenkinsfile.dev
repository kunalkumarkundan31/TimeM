
pipeline {
  agent any
  stages {
    stage('Unit Test') {
      steps {
        sh 'mvn clean test'
      }
    }
   
    stage('Deploy CloudHub') {
      environment {
        ANYPOINT_CREDENTIALS = credentials('anypoint.credentials')
      }
      steps {
        sh 'mvn deploy -P cloudhub -Dmule.version=4.3.0 -DmuleDeploy -Ddeployment=cloudhub -Danypoint.username=kunalkumarkundan007 -Danypoint.password=Dell@1234'
      }
    }
  }
}
